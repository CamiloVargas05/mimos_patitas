1. Para que funcione la BD se debe meter este .rar dentro de xampp/htdocs
2. Dentro de phpMyAdmin crear una BD llamada mimos_patitas. Despues, en la carpeta BD hay un archivo llamado mimos_patitas.sql y se debe importar en phpmyadmin (importa las tablas)